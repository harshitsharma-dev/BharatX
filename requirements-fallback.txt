# Price Comparison API - Fallback Requirements  
# Ultra minimal for maximum cloud platform compatibility
# No compiled dependencies

# Core web framework
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0

# HTML parsing - built-in parser only
beautifulsoup4==4.12.2

# Async HTTP
aiohttp==3.9.1

# Text similarity - pure Python (no Levenshtein)
fuzzywuzzy==0.18.0

# Production WSGI server
gunicorn==21.2.0

# Environment configuration  
python-dotenv==1.0.0

# Note: All compiled packages removed (lxml, python-Levenshtein, cchardet, etc.)
# App uses fallbacks: html.parser, basic string comparison
